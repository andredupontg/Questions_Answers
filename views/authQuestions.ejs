<%- include("partials/header") %>

<main role="main" class="inner cover">
    <h1 class="cover-heading">Tienes alguna pregunta en mente?</h1>
    <form class="formulario" action="/authSearchQuestion" method="post">
        <div class="form-floating">
            <input type="text" class="form-control" name="pregunta" id="floatingInput" placeholder="Titulo basado en Keywords unidos con '_'" autocomplete="off">
        </div>
        <button class="w-100 btn btn-lg btn-primary botones" type="submit">Buscar</button>
    </form>
    <form class="formulario" method="POST" action="/authQuestions">
        <div class="form-floating">
            <input type="text" class="form-control" name="title" id="floatingInput" placeholder="Titulo basado en Keywords unidos con '_'" autocomplete="off">
            <input type="text" class="form-control" name="content" id="floatingInput" placeholder="Contenido" autocomplete="off">
        </div>
        <button class="w-100 btn btn-lg btn-primary botones" type="submit">Crear</button>
    </form>
    <% questions.forEach((question) => {%>
        <div class="card pregunta" style="width: 25rem;">
            <div class="card-header titulo">
                <%=question.title%>
            </div>
            <ul class="list-group list-group-flush">
                <li class="list-group-item contenido"><%=question.content%> <a class="detalle" href="/authQuestions/:<%=question.title%>">Detalle</a></li>
            </ul>
        </div>
    <%})%>
</main>

<%- include("partials/footer") %>